CREATE TABLE
`ga4-analytics-352613.flattened_dataset.flattened_daily_ga_data_20221101`
  (
     `event_date`                      STRING,
     `user_pseudo_id`                  STRING,
     `event_name`                      STRING,
     `event_timestamp`                 INTEGER,
     `unique_session_id`               STRING,
     `page_title`                      STRING,
     `page_location`                   STRING,
     `cleaned_page_location`           STRING,
     `ga_sessionid`                    INTEGER,
     `ga_session_number`               INTEGER,
     `primary_publishing_organisation` STRING,
     `page_referrer`                   STRING,
     `cleaned_page_referrer`           STRING,
     `content_id`                      STRING,
     `browse_topic`                    STRING,
     `publishing_app`                  STRING,
     `public_updated_at`               STRING,
     `updated_at`                      STRING,
     `first_published_at`              STRING,
     `taxonomy_all_ids_deprecated`     STRING,
     `status_code`                     INTEGER,
     `withdrawn`                       STRING,
     `document_type`                   STRING,
     `history`                         STRING,
     `taxonomy_main_id`                STRING,
     `taxonomy_all_deprecated`         STRING,
     `taxonomy_main`                   STRING,
     `taxonomy_level_1`                STRING,
     `full_taxonomy`                   STRING,
     `full_taxonomy_ids`               STRING,
     `rendering_app`                   STRING,
     `organisations`                   STRING,
     `session_engaged`                 STRING,
     `schema_name`                     STRING,
     `method`                          STRING,
     `engagement_time_msec`            INTEGER,
     `event_campaign`                  STRING,
     `content_language`                STRING,
     `type`                            STRING,
     `event_source`                    STRING,
     `session_source`                  STRING,
     `link_text`                       STRING,
     `term`                            STRING,
     `link_url`                        STRING,
     `event_medium`                    STRING,
     `session_medium`                  STRING,
     `link_domain`                     STRING,
     `event_previous_timestamp`        INTEGER,
     `event_bundle_sequence_id`        INTEGER,
     `event_server_timestamp_offset`   INTEGER,
     `user_id`                         STRING,
     `user_first_touch_timestamp`      INTEGER,
     `category`                        STRING,
     `mobile_brand_name`               STRING,
     `mobile_model_name`               STRING,
     `mobile_marketing_name`           STRING,
     `mobile_os_hardware_model`        STRING,
     `operating_system`                STRING,
     `operating_system_version`        STRING,
     `language`                        STRING,
     `is_limited_ad_tracking`          STRING,
     `time_zone_offset_seconds`        INTEGER,
     `device_browser`                  STRING,
     `device_browser_version`          STRING,
     `browser`                         STRING,
     `browser_version`                 STRING,
     `hostname`                        STRING,
     `continent`                       STRING,
     `country`                         STRING,
     `region`                          STRING,
     `city`                            STRING,
     `sub_continent`                   STRING,
     `metro`                           STRING,
     `first_user_campaign`             STRING,
     `first_user_medium`               STRING,
     `first_user_source`               STRING,
     `stream_id`                       STRING,
     `platform`                        STRING,
     `event_dimensions_hostname`       STRING,
     `full_link_url`                   STRING,
     `search_sort`                     STRING,
     `search_term`                     STRING,
     `search_results`                  INTEGER,
     `ui_text`                         STRING,
     `section`                         STRING,
     `action`                          STRING,
     `index`                           INTEGER,
     `index_total`                     INTEGER,
     `political_status`                STRING,
     `publishing_government`           STRING,
     `world_locations`                 STRING,
     `dclid`                           STRING,
     `debug_mode`                      INTEGER,
     `engaged_session_event`           INTEGER,
     `entrances`                       INTEGER,
     `firebase_conversion`             INTEGER,
     `gclid`                           STRING,
     `gclsrc`                          STRING,
     `ignore_referrer`                 STRING,
     `content`                         STRING,
     `campaign_id`                     STRING,
     `index_section`                   INTEGER,
     `index_section_total`             INTEGER,
     `tool_name`                       INTEGER,
     `response`                        INTEGER,
     `ab_test`                         STRING,
     `navigation_list_type`            STRING,
     `navigation_page_type`            STRING,
     `percent_scrolled`                INTEGER,
     `sfmc_activity_id`                STRING,
     `sfmc_activity_name`              STRING,
     `sfmc_asset_id`                   STRING,
     `sfmc_channel`                    STRING,
     `sfmc_journey_id`                 STRING,
     `sfmc_journey_name`               STRING,
     `step_navs`                       STRING,
     `manual_campaign_id`              STRING,
     `manual_campaign_name`            STRING,
     `manual_source`                   STRING,
     `manual_medium`                   STRING,
     `manual_term`                     STRING,
     `manual_content`                  STRING,
     `is_active_user`                  BOOLEAN,
     `video_title`                     STRING,
     `video_url`                       STRING,
     `video_duration`                  INTEGER,
     `video_current_time`              INTEGER,
     `video_percent`                   INTEGER,
     `cookie_banner`                   STRING,
     `devolved_nations_banner`         STRING,
     `intervention`                    STRING,
     `phase_banner`                    STRING,
     `outbound`                        STRING,
     `query_string`                    STRING,
     `spelling_suggestion`             STRING,
     `outcome`                         STRING,
     `viewport_size`                   STRING,
     `item_list_name`                  STRING,
     `item_id`                         STRING,
     `item_name`                       STRING,
     `item_list_index`                 STRING,
     `item_content_id`                 STRING
  );
